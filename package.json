{
    "name": "ws-relay-server",
    "version": "2.4.0",
    "type": "module",
    "description": "WebSocket relay with AI processing — empty-return fix, thinking budget, google search, round-robin proxy",
    "main": "server.js",
    "scripts": {
        "start": "node server.js"
    },
    "dependencies": {
        "ws": "^8.18.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}